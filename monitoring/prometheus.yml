global:
  scrape_interval: 5s
  evaluation_interval: 5s
  external_labels:
    monitor: "ray-benchmark"

scrape_configs:
  - job_name: "benchmark-metrics"
    static_configs:
      - targets: ["localhost:8003"]
    scrape_interval: 5s
    scrape_timeout: 5s
